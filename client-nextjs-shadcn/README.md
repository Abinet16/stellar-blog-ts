# Blog Client (Next.js + TypeScript + Tailwind + shadcn-style)

1. Install dependencies:
   ```bash
   npm install
   ```
2. Copy `.env.example` to `.env.local` and adjust `NEXT_PUBLIC_API_URL` to point at your server.
3. Run:
   ```bash
   npm run dev
   ```
4. Open http://localhost:3000
